profile/
venv/
.venv/
.env
.relay.pid
__pycache__/
*.pyc
*.log
.bak*/
# 手工改代码前留的单文件备份（app.py.bak_20260904135416 这种）。上面的
# .bak*/ 只匹配目录名，匹配不到这类文件名后缀，漏进仓库就是把同一份代码的
# 中间态也公开出去。
*.bak_*

# 运行时产物：actions.log 是你的真实操作史（含发帖正文），绝不能进仓库
actions.log
runtime-state.json
